# [수정 완료·배포 확인 대기] 캘린더 월 이동 안정화

## 목적과 완료 조건

- 모바일에서 캘린더 위를 위아래로 움직여도 페이지가 자연스럽게 스크롤된다.
- 좌우 스와이프 의도가 확인되면 세로 이동보다 월 전환을 우선한다.
- 취소된 터치와 빠른 연속 스와이프에서도 제스처 상태와 이동 월이 꼬이지 않는다.
- 모바일 당겨서 새로고침 이후에도 다음 좌우 스와이프가 정상 작동한다.
- PC에서도 `/?year=YYYY&month=M` 주소를 새로고침한 뒤 휠 월 이동이 정상 작동한다.
- 쿼리 주소 직접 진입, 새로고침, 뒤로가기에서도 표시 월과 URL이 일치한다.
- 터치 로직을 캘린더 화면 코드와 분리해 기준값을 안전하게 조정하고 테스트할 수 있게 한다.

## 확정 범위

- `CalendarGrid`의 `touch-pan-y`를 유지해 세로 페이지 스크롤 허용
- 최초 약 10px 이동 후 가로·세로 의도 잠금
- 가로 이동량이 세로 이동량의 110% 이상일 때 좌우 제스처로 판정
- 가로 이동 45px 이상에서 한 달 이동
- `Calendar` 내부의 표시 월을 단일 상태로 관리하고 URL 쿼리는 초기값·공유 주소로 사용
- 모바일 스와이프, PC 휠, 이전·다음·오늘 이동이 같은 월 변경 함수에서 표시 상태와 URL을 함께 갱신
- URL 쿼리 주소 직접 진입과 브라우저 뒤로가기 시 표시 월을 다시 동기화
- URL 반영 전 연속 스와이프도 마지막 요청 월을 기준으로 계산
- UI 컴포넌트 추가 없이 캘린더 전용 훅과 순수 판정 함수로 분리

## 제외 범위

- 달력 디자인, 셀 높이, 축제 막대와 날짜 선택 동작 변경
- PC 마우스 휠 임계값과 모바일 스와이프 판정 기준 변경
- 스와이프 애니메이션 추가
- URL의 `year`, `month` 형식 변경

## 예상 수정 파일·구조

- `components/calendar/useCalendarSwipe.ts`: 현재 Pointer Events 제스처 판정 유지
- `lib/calendarSwipe.ts`: 이동 거리, 방향 비율과 최소 거리로 좌우 스와이프 판정
- `components/calendar/CalendarGrid.tsx`: 훅에서 받은 포인터 이벤트 연결
- `components/Calendar.tsx`: 표시 월 단일 상태, 쿼리 초기화·동기화, 공통 월 이동 함수
- `tests/calendarSwipe.test.ts`: 가로·세로·대각선·임계값 판정 검증
- `tests/calendarSelection.test.ts`: 연말·연초 및 연속 월 이동 계산 검증
- `ARCHITECTURE.md`, `PROJECT_STATUS.md`: 구조와 검증 상태 기록

데이터 흐름은 URL 쿼리 직접 진입 → 표시 월 초기화, 그리고 모바일 스와이프·PC 휠·버튼 → 공통 월 이동 함수 → 표시 월 즉시 변경 → URL 동기화 순서다.

## 작업 순서

- [x] 좌우 스와이프 순수 판정 함수와 단위 테스트 작성
- [x] 캘린더 전용 제스처 훅에 방향 잠금과 `pointercancel` 정리 구현
- [x] `CalendarGrid`와 `Calendar`에서 기존 인라인 포인터 로직을 훅으로 교체
- [x] URL 반영 전 연속 스와이프 월 기준 보정
- [x] 관련 구조 문서와 프로젝트 현황 갱신
- [x] 관련 테스트, 전체 테스트, 타입 검사와 변경 파일 린트 실행
- [ ] 실제 모바일에서 세로 스크롤·좌우 전환·빠른 연속 전환 확인
- [x] 당겨서 새로고침 이후 좌우 전환 불가 재현 원인 보강
- [x] 명시적 포인터 캡처 제거와 `lostpointercapture`·페이지 생명주기 초기화 보강
- [x] URL 쿼리에서 파생하던 표시 월을 `Calendar` 내부 단일 상태로 변경
- [x] 모바일·PC 공통 월 이동 함수에서 화면 상태와 URL을 함께 갱신
- [x] 쿼리 주소 직접 진입·새로고침·뒤로가기 회귀 테스트 추가
- [ ] `/`, `/?year=2026&month=8`에서 PC 휠과 모바일 스와이프 실기기 확인

## 회귀 위험과 검증 방법

- 대각선 세로 스크롤이 월 전환으로 오인되지 않는지 방향 비율 테스트와 실제 모바일로 확인한다.
- 당겨서 새로고침이 캘린더 포인터 캡처와 충돌하지 않는지 실제 모바일에서 확인한다.
- 짧은 탭과 날짜 선택이 스와이프로 오인되지 않는지 45px 임계값을 테스트한다.
- 스크롤 중 `pointercancel` 이후 다음 스와이프가 정상인지 확인한다.
- 빠르게 두 번 넘겼을 때 같은 달을 반복 요청하지 않고 두 달 이동하는지 확인한다.
- 이전·다음 달 버튼, 오늘 버튼, 브라우저 뒤로가기와 URL 직접 접근이 유지되는지 확인한다.
- 쿼리 URL을 새로고침한 뒤에도 PC 휠과 모바일 스와이프가 같은 월 변경 함수를 정상 호출하는지 확인한다.

## 후속 개선점

- 실제 기기 사용감에 따라 방향 비율 110%와 거리 45px는 별도 승인 후 조정한다.

## 구현 결과

- `useCalendarSwipe` 훅으로 포인터 시작·이동·종료·취소와 방향 잠금 상태를 분리했다.
- 10px 이후 한 축이 다른 축보다 110% 이상 우세할 때만 방향을 잠그고, 가로 45px 이상에서 월을 이동한다.
- `pointercancel`에서 제스처 상태를 초기화해 세로 스크롤 이후 다음 좌우 스와이프가 영향을 받지 않게 했다.
- URL 반영 전에도 별도 월 커서를 즉시 갱신해 빠른 연속 스와이프가 같은 달을 반복 요청하지 않게 했다.
- 최초 구현 당시 관련 테스트와 전체 테스트 173개, 타입 검사와 변경 파일 ESLint가 통과했다.
- 실제 모바일 화면 확인은 남아 있다.
- 실제 확인에서 당겨서 새로고침 후 좌우 스와이프가 작동하지 않는 문제가 재현됐다.
- 첫 추가 수정에서 가로 방향 확정 후 명시적 캡처를 시도했지만 실기기에서 좌우 스와이프가 전혀 작동하지 않는 회귀가 확인됐다.
- 모바일 직접 조작에는 암묵적 포인터 캡처가 적용되므로 명시적 `setPointerCapture`와 캡처 실패 시 제스처 초기화를 완전히 제거한다.
- `pointercancel`, `lostpointercapture`, `visibilitychange`, `pageshow`와 컴포넌트 해제 시 좌표·방향·캡처 상태를 초기화한다.
- 명시적 포인터 캡처와 캡처 실패 시 좌표 초기화를 제거하고 방향 판정, `pointercancel`, `lostpointercapture`, 페이지 생명주기 초기화만 유지했다.
- 관련 테스트 13개, 전체 테스트 173개, 타입 검사와 변경 파일 ESLint가 통과했으며 실제 모바일 재확인이 남아 있다.
- 추가 재현에서 모바일뿐 아니라 PC도 `/?year=2026&month=M` 주소를 새로고침하면 월 이동이 실패했다. 따라서 Pointer Events 문제가 아니라 쿼리 URL 최초 진입 시 표시 월과 공통 월 이동 상태의 초기화 문제로 범위를 수정했다.
- `year`, `month` 쿼리는 공유·새로고침 복원·뒤로가기를 위해 유지하고, 표시 월은 `Calendar` 내부 상태로 분리했다.
- 최초 진입에서는 쿼리를 초기값으로만 읽고, 월 이동은 화면 상태를 먼저 바꾼 뒤 URL을 갱신하며, 브라우저 뒤로가기는 `popstate`에서 다시 동기화한다.
- 관련 테스트 15개, 전체 테스트 175개, 타입 검사와 변경 파일 ESLint가 통과했다. 실제 배포 주소의 쿼리 새로고침 후 PC 휠·모바일 스와이프 확인은 남아 있다.
