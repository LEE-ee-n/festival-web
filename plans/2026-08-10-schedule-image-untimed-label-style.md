# [완료] 2026-08-10 일정 이미지 시간 미정 라벨 정리

## 목적과 완료 조건

- `시간 미정` 라벨 색상을 시간축 숫자와 같은 색상으로 맞춘다.
- 라벨을 `시간`과 `미정` 두 줄로 표시해 시간축 영역 안에 자연스럽게 배치한다.

## 확정 범위와 제외 범위

- 포함: 혼합 일정 이미지의 시간 미정 축 라벨 색상과 줄바꿈
- 제외: 시간 미정 카드와 무대별 배치, 타임테이블 데이터 변경

## 예상 수정 파일과 작업 순서

- `components/schedule-image/ScheduleImageCanvas.tsx`
  1. 라벨 색상을 시간 숫자와 같은 `theme.mutedText`로 변경한다.
  2. SVG `tspan`을 사용해 `시간` / `미정` 두 줄로 정렬한다.
  3. 타입 검사, 관련 ESLint와 전체 테스트를 실행한다.

## 회귀 위험과 검증 방법

- 두 줄 라벨이 마지막 시간 숫자나 시간 미정 카드와 겹치지 않는지 확인한다.
- 시간 미정 카드의 무대별 배치는 그대로 유지되는지 확인한다.

## 후속 개선점

- 없음.

## 실제 결과

- `시간 미정` 라벨을 시간축 숫자와 동일한 `theme.mutedText`, 24px, 700 굵기로 변경했다.
- SVG `tspan`을 사용해 `시간`과 `미정`을 두 줄로 중앙 정렬했다.
- TypeScript 검사, 관련 파일 ESLint, 전체 자동 테스트 258개와 `git diff --check`가 통과했다.
- 2026-08-11 로그인된 localhost 97번 화면에서 시간축과 같은 색상의 `시간` / `미정` 두 줄 라벨과 카드 간격을 실제 확인했다.
